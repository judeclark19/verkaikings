[build]
  command = "npm install && npm run build"
  publish = ".next"
[functions]
  directory = "functions"
  node_bundler = "esbuild"
  included_files = ["!functions/.eslintrc"]
  external_node_modules = ["firebase-functions", "firebase-admin"]