[build]
  command = "npm install && npm run build"
  publish = ".next"
[functions]
  directory = "functions"
  node_bundler = "esbuild"
  excluded_files = [".eslintrc.js"]
  external_node_modules = ["firebase-functions", "firebase-admin"]