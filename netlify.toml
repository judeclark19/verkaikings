[build]
  command = "npm install && npm run build"
  publish = ".next"
[functions]
  directory = "functions"
  node_bundler = "esbuild"

  # Ensure Netlify installs the required dependencies
  external_node_modules = ["firebase-functions", "firebase-admin"]